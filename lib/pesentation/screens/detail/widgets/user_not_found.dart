import 'package:flutter/material.dart';

class UserNotFoundArticle extends StatelessWidget {
  const UserNotFoundArticle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Пользователя не найдено.");
  }
}
